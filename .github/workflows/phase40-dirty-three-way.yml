name: Phase 40 Dirty Three-Way Recovery

on:
  push:
    branches: ["bugfix/phase40-dirty-three-way-recovery"]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: "npm"
      - name: Apply dirty three-way recovery hardening
        run: python scripts/phase40_dirty_three_way_validate.py
      - name: Validate dirty three-way recovery hardening
        run: |
          npm ci
          npm audit --omit=dev --audit-level=high
          npm run check
          npx cap sync ios
          git diff --exit-code -- ios
          git diff --check
      - name: Commit source and tests
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/App.tsx src/lib/i18n.ts src/__tests__/App.native-durable-snapshot.test.tsx
          git commit -m "Phase 40: preserve dirty screen across three-way recovery"
          git push origin HEAD:bugfix/phase40-dirty-three-way-recovery
