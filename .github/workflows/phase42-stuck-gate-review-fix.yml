name: Phase 42 Stuck Gate Review Fix

on:
  push:
    branches: ["bugfix/phase42-native-startup-divergence"]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: "npm"
      - name: Apply review fix
        run: python scripts/phase42_stuck_gate_fix.py
      - name: Validate Phase 42 review fix
        run: |
          npm ci
          npm audit --omit=dev --audit-level=high
          npm run check
          npx cap sync ios
          git diff --exit-code -- ios
          git diff --check
      - name: Commit source and regression
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/App.tsx src/__tests__/App.native-durable-snapshot.test.tsx
          git commit -m "Phase 42: recover from local changes during native probe"
          git push origin HEAD:bugfix/phase42-native-startup-divergence
