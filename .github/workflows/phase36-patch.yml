name: Phase 36 No-op Pending Cancellation Patcher

on:
  push:
    branches: ["bugfix/phase36-noop-pending-cancel"]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: "npm"
      - name: Apply patch
        run: python scripts/phase36_patch.py
      - name: Validate implementation
        run: |
          npm ci
          npm audit --omit=dev --audit-level=high
          npm run check
          npx cap sync ios
          git diff --exit-code -- ios
      - name: Commit source changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src
          git status --short
          git commit -m "Phase 36: harden no-op saves and pending cancellation"
          git push origin HEAD:bugfix/phase36-noop-pending-cancel
